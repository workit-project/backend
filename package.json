{
  "name": "backend",
  "version": "0.0.1",
  "description": "workit backend",
  "main": "index.js",
  "scripts": {
    "start": "node -e \"require('dotenv').config({}); require('.').listen(4000)\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Chance Hudson",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^5.7.0"
  },
  "devDependencies": {
    "dotenv": "^8.1.0"
  }
}
